<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Blog MVC{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <nav class="bg-blue-600 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-white text-2xl font-bold">Blog MVC</a>
            <div class="space-x-4">
                <a href="/article" class="text-white hover:text-blue-200">Articles</a>
                {% if session.logged_in %}
                    <a href="/article/create" class="text-white hover:text-blue-200">Create Article</a>
                    <a href="/logout" class="text-white hover:text-blue-200">Logout</a>
                {% else %}
                    <a href="/login" class="text-white hover:text-blue-200">Login</a>
                    <a href="/register" class="text-white hover:text-blue-200">Register</a>
                {% endif %}
            </div>
        </div>
    </nav>

    <div class="container mx-auto mt-8 px-4">
        {% block content %}{% endblock %}
    </div>

    <footer class="bg-blue-800 text-white text-center p-4 mt-8">
        <p>&copy; {{ "now"|date("Y") }} Blog MVC. All rights reserved.</p>
    </footer>
</body>
</html>